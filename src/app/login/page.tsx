import LoginForm from "@/components/loginForm";
import React from "react";

const LoginPage = () => {
  return (
    <div className="login">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
